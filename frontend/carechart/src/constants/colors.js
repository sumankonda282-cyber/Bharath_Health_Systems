// Single source of truth for brand colors.
// CSS variables are defined in index.css — these constants reference them.
// Usage: style={{ color: GREEN }} or style={{ background: NAVY }}
export const GREEN = 'var(--green)'
export const NAVY  = 'var(--navy)'
export const RED   = 'var(--red)'
